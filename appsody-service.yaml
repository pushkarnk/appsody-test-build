apiVersion: serving.knative.dev/v1alpha1
kind: Service
metadata:
  name: appsody-build-test
spec:
  runLatest:
    configuration:
      revisionTemplate:
        spec:
          container:
            image: localhost:5000/appsody-build-test
            imagePullPolicy: Always
            ports:
            - containerPort: 3000
